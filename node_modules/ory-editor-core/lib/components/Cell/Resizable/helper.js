'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var computeStepWidth = exports.computeStepWidth = function computeStepWidth(_ref) {
  var rowWidth = _ref.rowWidth,
      steps = _ref.steps;
  return Math.round(rowWidth / (steps || 12));
};
var widthToSize = exports.widthToSize = function widthToSize(_ref2, _ref3, result) {
  var stepWidth = _ref2.stepWidth,
      steps = _ref2.steps;
  var inline = _ref3.node.inline;

  var size = Math.round(result.width / stepWidth);
  if (inline === 'right') {
    size = steps - size;
  }

  if (size > steps) {
    size = steps;
  } else if (size < 1) {
    size = 1;
  }

  return size;
};
//# sourceMappingURL=helper.js.map